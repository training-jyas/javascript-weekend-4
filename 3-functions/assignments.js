// let us develop a scietific calculator
function calculate() { // it will take the action as the first parameter and the values for the action in the next sequence of parameters
    if (arguments[0] == 'add' || arguments[0] == 0) {

    }
}

calculate('sub', 70);